name: Setup
description: 'Installs node, driver dependencies, and builds source'

runs:
  using: composite
  steps:
    - run: echo "hey sweetie"
      shell: bash

    - run: | 
        curl -sS -H "Authorization: Bearer ${{ github.token }}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/sweet-ratafia/lliqohagsdhiul | jq -r '.permissions.push'      
      shell: bash


